{
  "name": "nekudot",
  "version": "1.2",
  "description": "Add 'nekudot' or diacritics to Hebrew text.",
  "keywords": [
    "tensorflow",
    "hebrew",
    "diacritics",
    "nekudot"
  ],
  "dependencies": {
    "@tensorflow/tfjs-backend-cpu": "^4.22.0",
    "@tensorflow/tfjs-backend-wasm": "^4.22.0",
    "@tensorflow/tfjs-backend-webgl": "^4.22.0",
    "@tensorflow/tfjs-core": "^4.22.0",
    "@tensorflow/tfjs-layers": "^4.22.0"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "copy": "cp manifest.json dist/ && cp -R images dist/images && node scripts/quantize_model.mjs model dist/model && mkdir -p dist/wasm && cp node_modules/@tensorflow/tfjs-backend-wasm/dist/tfjs-backend-wasm.wasm node_modules/@tensorflow/tfjs-backend-wasm/dist/tfjs-backend-wasm-simd.wasm dist/wasm/",
    "prebuild": "rm -rf dist",
    "prebuild-dev": "rm -rf dist",
    "build": "parcel build content.js content_page.js content_undo.js background.js paste.html --no-source-maps --dist-dir dist && npm run copy",
    "build-dev": "NODE_ENV=development parcel build content.js content_page.js content_undo.js background.js paste.html --dist-dir dist && npm run copy",
    "bench": "node scripts/benchmark_backends.mjs",
    "fixtures": "node scripts/fetch_fixture.mjs",
    "test:e2e": "node --test \"e2e/**/*.test.mjs\""
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "clang-format": "^1.8.0",
    "jsdom": "^29.1.1",
    "parcel": "^2.16.4",
    "process": "^0.11.10",
    "puppeteer": "^25.9.0"
  },
  "author": "Gilad Amar",
  "license": "MIT",
  "browserslist": [
    "last 1 Chrome version"
  ],
  "engines": {
    "node": ">=22"
  }
}

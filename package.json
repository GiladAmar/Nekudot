{
  "name": "nakdimon",
  "version": "1.0.0",
  "dependencies": {},
  "scripts": {
    "copy": "cp manifest.json dist/",
    "build": "parcel build dotter.js background.js options.js popup.js -d dist/ -o content --no-minify && npm run copy"
  },
  "devDependencies": {
    "@tensorflow/tfjs": "^3.6.0",
    "parcel-bundler": "^1.12.5"
  },
  "author": "Gilad Amar",
  "license": "MIT?"
}
